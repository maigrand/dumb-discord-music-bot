version: '3.9'

services:
  redis:
    image: 'redis'
    ports:
      - '6379:6379'
    volumes:
      - ./redis-data:/data
    restart: unless-stopped
  dumbbot:
    depends_on:
      - redis
    restart: unless-stopped
    build: .
