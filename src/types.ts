export type Track = {
    videoId: string
    title: string
    channelTitle: string
    duration: number
    url: string
    requesterUserId: string
}

export type TrackMetadata = {
    guildId: string
}
